{
  "name": "english-as-code",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "golden": "tsc -p tsconfig.json && node dist/test/goldenRunner.js",
    "dev:api": "tsc -p tsconfig.json && node dist/api/server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10"
  }
}
